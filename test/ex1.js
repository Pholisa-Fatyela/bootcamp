//const assert = require('assert');
var assert = chai.assert;

describe('calculate call duration', function() {
    //
    it('should find the total duration of all the calls', function() {

        // define a variable for each call duration -
        // you should have 4 call duration variables

        //sum all 4 variables into one variable called `totalCallDuration`

        var totalCallDuration = null;
        assert.equal(100, totalCallDuration);
    });

    it('should find the cost for each call', function() {

        // define 4 variables that contains each call duration

        var totalCallDuration = null;
        assert.equal(call1Cost, 0);
        assert.equal();
        assert.equal();
        assert.equal();

    });

    it('should find the total cost for all calls', function() {

        // define 4 variables that contains each call duration

        var totalCallCost = null;
        assert.equal(totalCost, 0);

    });
});
